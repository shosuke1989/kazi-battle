<div class="main user-show">
  <div class="container">
    <div class="container">
      <h1 class="form-heading">マイページ</h1>
        <div class="form">
          <div class="form-body">
          <%= @firstname.name %>
          <%= link_to("編集", "/firstnames/#{@firstname.id}/edit") %>
          <%= link_to("削除", "/firstnames/#{@firstname.id}/destroy",{method: "post"}) %>


          </div>
        </div>
    </div>
  
    <h1 class="form-heading">家族一覧</h1>


    <%= debug @graph %>
    <%= debug @doits %>

      <%= bar_chart @graph, width: "800px", height: "500px"%>

      
    </div>

    
</div>

